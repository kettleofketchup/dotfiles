# ghostty overrides for the Mac. Stowed with: stow -d ~/dotfiles -t ~ mac
# Included from ~/.config/ghostty/config via `config-file = ?local.conf`.
# Keys here override the shared config, so only put genuine differences below.
